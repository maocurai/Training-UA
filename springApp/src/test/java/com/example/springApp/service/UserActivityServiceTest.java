package com.example.springApp.service;

import com.example.springApp.domain.Status;
import com.example.springApp.domain.UserActivity;
import org.junit.jupiter.api.Test;
import org.springframework.beans.factory.annotation.Autowired;

import static org.junit.jupiter.api.Assertions.*;

class UserActivityServiceTest {

    @Autowired
    private UserActivityService userActivityService;

//    @Test
//    void doActionDueToStatus() {
//        UserActivity userActivity = new UserActivity();
//        userActivityService.save(userActivity);
//        Long id = userActivityService.
//        userActivityService.doActionDueToStatus(userActivity, Status.DELETE_REQUEST);
//
//    }
}